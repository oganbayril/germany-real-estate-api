"""Load the latest artifact and score a single feature record. Used by the API. Phase 4."""

from __future__ import annotations

# TODO(phase-4): PricePredictor.load_latest(); .predict_one(record) -> PredictionResult
