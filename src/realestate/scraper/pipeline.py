"""Scrape orchestration. Phase 2.

For each city: walk search pages -> fetch exposes -> upsert -> log the run.
"""

from __future__ import annotations

# TODO(phase-2): run_scrape(cities, max_pages) writing to scrape_runs + listings
